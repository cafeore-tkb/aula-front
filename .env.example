# REST API base URL. Omit this variable when the API is served from /api/v1
# on the same origin as the frontend.
VITE_API_BASE_URL=/api/v1

# Local development only. This bypass is always disabled in production builds.
VITE_DEV_MODE=true
